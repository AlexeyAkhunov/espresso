/* exported */
extern sm_row *sm_minimum_cover(sm_matrix * A, int * weight,
                                int heuristic /* set to 1 for a heuristic covering */,
                                int debug_level /* how deep in the recursion to provide info */);
